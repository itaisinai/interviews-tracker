services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: jobcrm
      POSTGRES_PASSWORD: jobcrm
      POSTGRES_DB: jobcrm
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
